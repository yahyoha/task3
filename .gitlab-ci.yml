stages:
  - deploy_dashboard

deploy_dashboard:
  stage: deploy_dashboard
  image: grafana/grafana-cli
  script:
    - grafana-cli login --api-key=$GRAFANA_API_KEY
    - grafana-cli dashboards import --file=grafana/CloudBillingDashboard.json --folder=BillingDeployr --overwrite
  when: manual

